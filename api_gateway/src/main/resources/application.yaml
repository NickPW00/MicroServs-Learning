spring:
  application:
    name: api_gateway

  cloud:
    gateway:
      routes:

        - id: user-service
          uri: http://localhost:8081
          predicates:
            - Path=/users/**

        - id: product-service
          uri: http://localhost:8082
          predicates:
            - Path=/products/**

        - id: cart-service
          uri: http://localhost:8083
          predicates:
            - Path=/users/*/cart/**

server:
  port: 8080

